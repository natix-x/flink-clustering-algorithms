<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>clustering</groupId>
    <artifactId>flink-clustering</artifactId>
    <version>0.1</version>
    <packaging>jar</packaging>

    <name>flink-clustering-algorithms</name>
    <description>Flink (Java) side of the clustering benchmark. Mirrors the Spark repo's
        clustering.benchmark package and conforms to the shared JSON contract.</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Build with JDK 17 locally, emit Java 11 bytecode (Cyfronet runs Java 11). -->
        <maven.compiler.release>11</maven.compiler.release>
        <!-- Pinned to 1.17.x: Flink ML 2.3.0 (used for the iteration-based KMeans, impl=mliter)
             only ships artifacts for Flink 1.17. -->
        <flink.version>1.17.1</flink.version>
        <flinkml.version>2.3.0</flinkml.version>
        <jackson.version>2.15.2</jackson.version>
        <junit.version>5.10.2</junit.version>
    </properties>

    <dependencies>
        <!-- Flink: provided on the cluster module; available at compile+test, not bundled. -->
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-streaming-java</artifactId>
            <version>${flink.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-clients</artifactId>
            <version>${flink.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- Serves the local MiniCluster web dashboard (http://localhost:8081) when
             FLINK_WEB_UI=1. Provided: on the cluster the real JobManager serves it. -->
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-runtime-web</artifactId>
            <version>${flink.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- Metric reporter SPI (clustering.metrics.FileMetricReporter). Provided: the
             reporter is loaded by the Flink runtime (MiniCluster locally, $FLINK_HOME/lib
             on the cluster). -->
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-metrics-core</artifactId>
            <version>${flink.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Flink ML iteration framework (for impl=mliter). Bundled into the fat jar
             (it is NOT part of the Flink runtime). Its own Flink deps are provided. -->
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-ml-iteration-1.17</artifactId>
            <version>${flinkml.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-ml-core-1.17</artifactId>
            <version>${flinkml.version}</version>
        </dependency>

        <!-- JSON contract (bundled into the fat jar). -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <!-- Tests: run Flink locally via MiniCluster (provided deps are on the test classpath). -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Test-only SLF4J binding so local-run Flink logs (incl. metric-reporter setup)
             are visible instead of the no-op logger. -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>1.7.36</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>flink-clustering-benchmark</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>

            <!-- Fat jar (without provided Flink): clustering.benchmark.BenchmarkRunner -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.3</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>clustering.benchmark.BenchmarkRunner</mainClass>
                                </transformer>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>